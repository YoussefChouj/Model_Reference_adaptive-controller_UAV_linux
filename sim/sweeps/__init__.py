"""Sweep families for sim/experiments.py (sim-arch-03).

Each module exports a ``run`` function that accepts the scenario factory and
optional keyword arguments, writes structured artifacts to ``sweep_results/<ts>/``,
and returns a ``SweepResult``.
"""
