# AGENT_INDEX.md

**Single canonical entry point for any agent on this repo.** Run `python
scripts/build_agent_index.py` to refresh. The output file is **gitignored** —
generated, not authored.

> If you are an agent: read this file first, then jump to the row that matches
> your task. Do not start by grepping the tree.

## Working tree at a glance

| Question | Read |
|---|---|
| What is this project? | `docs/architecture.md`, `CLAUDE.md` Session State |
| Why was X built this way? | `docs/decisions.md`, `docs/adr/` |
| What current goal governs? | `CLAUDE.md` Session State, `.agent_contracts/` |
| What is the file ownership map (parallel-safe)? | `.agent_contracts/prior_transfer/README.md`, `docs/interfaces.md` |
| What does this folder own? | The folder's `README.md` (or this index if absent) |
| What are known gotchas? | `wiki/index.md`, `.agent_memory/lessons.jsonl` |

## Where to start by task type

| Task type | Start here |
|---|---|
| Pure read / search / lookup | This file → query → read the row. |
| Implement a change from a spec | `.agent_contracts/<id>/spec.md` (read spec) + `.agent_contracts/<id>/journal.md` (read history first). |
| Code review | `.agent_contracts/<id>/journal.md` + `.cursor/rules/project-context.mdc` |
| Add a new spec | `.cursor/skills/uav-planner/SKILL.md` (interactive planner) |
| Run the simulator | `sim/README.md`, then `.venv/bin/python sim/run.py --help` |
| Edit firmware (`API/`, `FreeRTOS/`, `USER/`, `TASK/`, `BSP/`) | **STOP.** Linux-native agents may not edit firmware. Read `.cursor/rules/hardware-safety.mdc` and consult the operator. |
| Make / refresh a graph | `.cursor/rules/000-core.mdc` §1, `.cursor/rules/knowledge-stack.mdc` |
| Update knowledge | `wiki/`, `sessions_summary/`. See `sessions_summary/POLICY.md`. |
| Diagnose a failing test | Run the test under `.venv/bin/python` first (full path). `.cursor/rules/environment.mdc` covers the 5 common failures. |

## Hard rules (always-on)

| Rule | Document |
|---|---|
| Knowledge stack first, then explore | `.cursor/rules/000-core.mdc` |
| Git on this box needs a timeout wrapper | `.cursor/rules/agent-environment.mdc` |
| Don't touch firmware paths from linux agents | `.cursor/rules/hardware-safety.mdc` |
| Push guard: this clone's `origin` is the **linux** remote | `.cursor/rules/agent-environment.mdc` |
| Session lifecycle | `.cursor/rules/session-lifecycle.mdc` |
| Embedded C conventions | `.cursor/rules/embedded-c.mdc` (auto-attaches on API/TASK/BSP/USER) |

## Knowledge layers (read in this order)

1. **`AGENT_INDEX.md`** (this file) — you are here
2. **`CLAUDE.md` Session State** — current goal + explicit user intent (highest authority)
3. **`.agent_contracts/`** — contract truth per task
4. **`docs/decisions.md`, `docs/adr/`** — decision rationale
5. **`docs/interfaces.md`** — cross-subsystem contracts
6. **`graphify-out/GRAPH_REPORT.md`** — code graph (when regenerated)
7. **`wiki/`** — domain knowledge + gotchas
8. **`.agent_memory/lessons.jsonl`** — past task failures in this codebase

If `graphify-out/` is empty or stale, regenerate: `.venv/bin/python tasks.py graph`.

## Subagent dispatch (model pins)

Configured models for this project — **do not override via Task `model:` param** without explicit user approval:

| Subagent | Model | Role |
|---|---|---|
| `uav-conductor` | `claude-opus-5-high` | Pipeline orchestrator |
| `uav-planner` | `claude-opus-5-high` | Spec author (interactive) |
| `uav-implementer` | `cursor-grok-4.6-high-fast` | Token-heavy execution |
| `uav-reviewer` | `gpt-5.6-sol-high` | Sanity check (must differ from implementer family) |

See `.cursor/rules/subagent-model-pinning.mdc`. If a subagent fails to dispatch
on its configured model, **stop and report** rather than silently falling back.

## Outputs (gitignored, regenerable)

| Output | Generated by | Lives at |
|---|---|---|
| Code graph | `python tasks.py graph` | `graphify-out/` |
| Repo index | `python scripts/build_agent_index.py` | `AGENT_INDEX.md` (this file) |
| Wiki knowledge | `python scripts/pull_wiki_inbox.py` + ingest | `wiki/` |
| Session digests | manual at session end | `sessions_summary/YYYY-MM-DD-digest.md` |
| Simulation runs | `python sim/run.py` | `sim/runs/` |

## Live snapshot (regenerated by `scripts/build_agent_index.py`)

- Local HEAD: `ff6ad34`
- `origin` URL: `git@github.com:YoussefChouj/Model_Reference_adaptive-controller_UAV_linux.git`
- Local branches: 13 (`git for-each-ref refs/heads/`)
- `.agent_contracts/` specs: 24
- `wiki/` markdown pages: 78
- `raw/papers/*.md` tracked: 7

### Specs on disk

- `.agent_contracts/prior-00-sign-gate`
- `.agent_contracts/prior-00b-sim-parity-fix`
- `.agent_contracts/prior-01-retire-gazebo`
- `.agent_contracts/prior-02-delay-wrapper`
- `.agent_contracts/prior-03-mujoco-plant`
- `.agent_contracts/prior-04-sysid-gate`
- `.agent_contracts/prior-05-prior-factory`
- `.agent_contracts/prior-06-injection-seam`
- `.agent_contracts/prior-07-rig-model`
- `.agent_contracts/prior-08-declared-basis-dimensions`
- `.agent_contracts/prior-08-regressor-dimensions`
- `.agent_contracts/prior-09-invariance-sweep`
- `.agent_contracts/prior-10-trajectory-scenarios`
- `.agent_contracts/prior-11-learning-vs-deployment-config`
- `.agent_contracts/prior-12-concurrent-learning`
- `.agent_contracts/prior-13-offline-prior-fitting`
- `.agent_contracts/prior-14-attention-stack-query`
- `.agent_contracts/sim-arch-02`
- `.agent_contracts/sim-arch-05`
- `.agent_contracts/sindy-adapter-px4-schema-modernisation`
- `.agent_contracts/sindy-real-flight-viewer`
- `.agent_contracts/smoke_test_20260725`
- `.agent_contracts/uart5_address_subscription_cmd`
- `.agent_contracts/uart5_livewatch_path`
