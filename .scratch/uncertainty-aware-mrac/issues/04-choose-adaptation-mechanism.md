# Choose the adaptation mechanism(s) to spec

Type: grilling
Status: open
Blocked by: 03

## Question

Given the survey (ticket 03), which mechanism(s) does the thesis spec commit to — and in what order (e.g., matrix Γ as the safe baseline, RLS-P as the contribution)? Decide: the mechanism(s), the stability argument each ships with (must-have gate), whether an estimator covariance signal (ticket 02) plays any role, and what the null hypothesis / baseline comparison is (current fixed scalar γ + deadzone).
