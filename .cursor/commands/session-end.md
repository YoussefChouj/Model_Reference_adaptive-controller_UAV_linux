# Session End

Wrap up this session.

1. Summarize what changed (files + why) and what was **verified** (build / tests / replay). Be honest about anything skipped or still owed.
2. Update the "Session State" block in `CLAUDE.md` (and `docs/progress.md` if present): goal, done, blocked, next-recommended.
3. If new, non-obvious codebase knowledge emerged (a root cause, a gotcha, a cross-subsystem relationship), add a short entry under `wiki/`.
4. Report in the `STATUS / COMPLETED / BLOCKED / FILES_CHANGED / TESTS / NEXT_RECOMMENDED / CONFIDENCE` format.
