# Session Start

Begin a work session on this repo.

1. Run `python scripts/pull_wiki_inbox.py` — syncs grabbed papers from the OpenClaw server and reports any awaiting wiki ingestion; surface pending papers to the user (offer ingest or a `grill-paper` session).
2. Read the "Session State" block in `CLAUDE.md` (and `docs/progress.md` if present) plus `.agent_memory/lessons.jsonl` — recall where we left off and past lessons.
3. Restate the current goal in one sentence and list the top 1–3 open items.
4. Propose a short plan as verifiable steps, then **wait for my confirmation before touching code**.

Follow the knowledge-stack-first protocol in `.cursor/rules`. Do not start editing until I confirm the goal.
